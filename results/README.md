# Execution results

These files record direct JSON output from successful local executions on
2026-09-05. They contain no credentials or generated model artifacts.

| POC | CPU | MPS |
| --- | --- | --- |
| Minimal Karate | [`karate-cpu.json`](karate-cpu.json) | [`karate-mps.json`](karate-mps.json) |
| Larger WikiCS | [`wikics-cpu.json`](wikics-cpu.json) | [`wikics-mps.json`](wikics-mps.json) |

The CUDA result will be produced separately by POC 3 on Kaggle. Its full
prediction artifact is intended for download and local Neo4j import, not source
control.
